import { sum } from "../src/file-2";

describe("Test file 2", () => {
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
  it("Should run function sucessfully", async () => {
    const response = await sum(1, 1);

    expect(response).toBe(2);
  });
});
